'use strict';

export const router: any = {
    config: require('./get.config'),
    reservation: require('./reservation'),
    student_info: require('./student-information'),
    teacher_info: require('./teacher-information'),
};
